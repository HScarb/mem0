# Mem0优化设计文档

## 背景

大模型的上下文窗口是有限的，在对话长度超过大模型的上下文窗口大小的时候，大模型无法保持对话的连贯性和一致性。记忆系统是目前的解决方案之一，其中mem0是一种可扩展的以内存为中心的架构，它通过从正在进行的对话中动态提取、评估和管理重要信息的方式进行记忆的提取和更新。但是mem0无法有效处理有时效性的信息，针对这个问题，我们希望引入事件记忆来提升 mem0 的性能。

### AI Memory

记忆是AI智能体必备的能力之一。随着对话轮数与深度的增加，对话上下文内容不断累计，很容易超过大模型的上下文长度限制。如果不对记忆进行优化，长对话很容易带来两个问题：**遗忘早期信息，过度消耗大量计算资源与成本。** 引入记忆系统能够有效解决上述问题，目前记忆分为短期记忆和长期记忆。

* **短期记忆：** 存储当前会话或任务的上下文信息，通常短暂存在以支持实时交互和决策。

* **长期记忆：** 持久存储用户偏好、历史交互或关键数据，用于跨会话的个性化响应和学习。

### Mem0

Mem0 是一个为 AI 智能体设计的开源记忆层，通过向量存储和自适应学习技术，跨会话、跨应用地保留用户偏好与交互历史，实现长期、动态、个性化的上下文记忆能力，相比传统 RAG 能更深刻地理解实体关系并保持信息时效性与连续性。系统提供全面的记忆管理接口，支持Add、Get、Search、Update、Delete和History，适配多种会话标识和智能提取方式，实现长期个性化记忆能力。

Mem0包含两个核心模块：Extraction Phase和Update Phase

* **Extraction Phase**：通过接收Agent和用户的Message，收集最近的"M"条的Message作为New Memories的上下文窗口，并将这段New Memories提取到Update Phase。
* **Update Phase**：接收到提取出来的Raw Memories后，该模块还会从向量数据库检索出"S"个最相似、最具关联性的历史记忆，并通过一个"tool call"的机制，将新增记忆和历史记忆进行比对，并交由大模型决策新增记忆的结果：增删改或者无操作。此后，对新获取的记忆的决策将直接更新到向量数据库。

### Memobase

Memobase是一个开源的长期内存后端，专为AI应用设计，用于存储结构化的用户配置文件和不断演变的内存事件，与传统的RAG不同，它专注于构建用户中心化的记忆系统，帮助AI代理记住用户偏好、行为模式和历史交互，从而实现更个性化和适应性的用户体验。亮点在于对记忆进行分类，分别是Profile和Event记忆。

Profile记忆是Memobase长期记忆中的核心组成部分，用于捕捉和维护用户的核心身份特征和属性，例如基本信息（如姓名、性别）、教育背景或其他自定义属性。Profile的结构定义：(1) 默认初始结构 (2) 允许用户配置 (3) 允许LLM修改

Event记忆专注于追踪用户生活中发生的事情，形成带时间戳的记录。Event的结构定义：(1) 摘要 (2) 标签：如情绪、人生目标、关系，由用户配置 (3) 创建时间 (4) 对应的个人资料

### LoCoMo

LoCoMo（Long-term Conversational Memory）评测集是为了**评估大型语言模型在处理和记忆超长对话能力**而创建的一个基准数据集。现有的很多对话模型在处理较短的对话时表现良好，但当对话变得非常长、跨越多个会话时，它们往往会"忘记"早期的信息，导致对话不连贯或回答错误。LoCoMo 旨在解决这个研究空白。

LoCoMo 的设计和构建包含了以下几个关键细节，让它能有效地评测模型的**长期记忆**：

1. **超长对话数据**: LoCoMo 包含**非常长**的对话，平均每个对话有大约 **300 个回合 (turns)**，总计约 **9,000 个 token**。这些对话被组织成多个会话 (sessions)，最多可达 **35 个会话**。这模拟了真实世界中用户与AI助手或与其他用户进行持续、长期的互动场景。
2. **全面的评测任务**: LoCoMo 并非仅仅提供对话数据，还构建了一个综合的评测基准。其中**问答**是最核心的评测任务：模型需要"回忆"过去的对话内容来回答问题。问题类型被进一步细分为：
    1. **单跳 (Single-hop)**: 直接从对话中找到答案。
    2. **多跳 (Multi-hop)**: 需要整合来自对话不同部分的信息才能回答。
    3. **时间性 (Temporal)**: 涉及时间顺序或时间点的理解。
    4. **开放知识 (Open Domain)**: 需要结合常识或背景知识。

## 概要设计

在新增记忆的步骤里面添加一个新增事件(Event)记忆的流程。在搜索记忆的步骤中并行搜索用户记忆和事件记忆，并将搜索结果进行整合。

Event 记忆的设计参考 Memobase 的设计。

### 新增记忆流程

在 add 方法中，新增 add_event_to_vector_store 函数（与 add_to_vector_store 并列，并行执行）。来添加 event 类型的记忆。

### 搜索记忆流程

在 search 方法中，新增 _search_event_vector_store 函数（与 _search_vector_store 并列，并行执行）。来搜索 event 类型的记忆。

## 详细设计

### 数据结构

### Event记忆的数据结构

| 参数 | 类型 | 说明 |
|------|------|------|
| `embeddings` | `List[float]` | 插入的向量列表。 |
| `memory_id` | `str` | 与对应向量相关的额外信息或元数据 |
| `metadata` | `dict` | metadata["data"] = data; metadata["hash"] = hashlib.md5(data.encode()).hexdigest(); metadata["created_at"] = datetime.now(pytz.timezone("US/Pacific")).isoformat() |

### 数据库

在 Memory 类中新增字段：event_vector_store，与 vector_store 类似，用于存储 Event 数据。

### 流程

> 包含改动后的流程图，标明修改的地方  
> 设计文档应该直接可以指导大模型进行编码

为两个类型的记忆设计两个隔离的向量数据库分别存储embedding。

#### add 改动(mem0\memory\main.py)

add 函数体内新增 add_event_to_vector_store 函数，参考 add_to_vector_store 实现

| 参数 | 类型 | 说明 |
|------|------|------|
| `messages` | `List[Dict[str, any]]` | 要存入向量数据库的消息列表。 |
| `metadata` | `Dict[str, any]` | 与这批消息关联的元数据。它是一个字典，包含了 `user_id`, `agent_id` 以及用户自定义的元数据。 |
| `filters` | `Dict[str, any]` | 用于在向量存储中进行过滤的条件。它是一个字典，通常包含 `user_id` 或 `agent_id` 等，以确保数据隔离和正确检索。 |
| `infer` | `bool` | 一个布尔标志。当为 `False` 时，函数直接添加消息到数据库中。当为 `True` 时，调用大模型整合消息。 |

add_event_to_vector_store 与 add_to_vector_store 函数大体上逻辑保持不变，关键改动是要实现Event记忆和用户记忆的隔离。

* 首先需要将系统提示词进行改动，在函数体内参考 get_fact_retrieval_messages函数新增get_event_retrieval_messages函数，实现EVENT_RETRIEVAL_PROMPT (具体实现参考下文)，用户输入内容保持不变。

* 对大模型处理过后的response进行解析，通过之前大模型解析好的结果获取event字段的内容，获得new_event_facts.

* 参考new_message_embeddings修改为new_event_embeddings，搜索的向量数据库替换为Memory类新增的event_vector_store。

##### 系统提示词改动 (mem0\configs\prompts.py)

保留系统提示词 FACT_RETRIEVAL_PROMPT , 在此基础上新增提示词EVENT_RETRIEVAL_PROMPT，并且修改提示词内部的few_shot_example，将原先的fact字段替换为event字段。

* FACT_RETRIEVAL_PROMPT 的内容主要是引导大模型记录用户个人信息的内容。

* EVENT_RETRIEVAL_PROMPT的内容用于引导大模型记录用户的行为以及发生行为的时间戳信息。可以参考FACT_RETRIEVAL_PROMPT 获取当前系统时间的方式。

提供memobase的系统提示词

```plaintext
"""你是一位从聊天记录中记录个人信息、日程安排和事件的专家。
你将获得用户和助手之间的对话内容。

## 要求
- 你需要列出用户信息和日程安排
- {additional_requirements}
- 如果用户事件/日程有具体的提及时间或者事件发生的时间。根据消息中的[TIME]在你提取的事件和日程中补充相关的时间信息。例如：
    输入: `[2024/04/30] user: 我昨天买了一辆新车！`
    输出: `用户买了一辆新车[提及于 2024/04/30, 买车在2024/04/29]。`
    输入: `[2024/04/30] user: 我4年前买了一辆车！`
    输出: `用户买了一辆车[提及于 2024/04/30, 买车在2020]。`
    说明: 因为你只知道年份而不知道具体日期，所以 2024-4=2020。或者你可以记录为 [2024/04/30之前4年]
    输入: `[2024/04/30] user: 我上周买了一辆新车！`
    输出: `用户买了一辆新车[提及于 2024/04/30, 买车在2024/04/30之前一周]。`
    说明: 因为你不知道具体日期。
    输入: `[...] user: 我上周买了一辆新车！`
    输出: `用户买了一辆新车。`
    说明: 因为你不知道具体日期，所以不要附加任何日期是错误的答案。

### 重要信息
以下是你应该从聊天中记录的主题/子主题。
<topics>
{topics}
</topics>
以下是你应该从聊天中记录的重要属性。
<attributes>
{attributes}
</attributes>

## 输入格式
### 已记录
你会收到一系列的已记录信息，你留意需要记录可能和已记录信息相关的信息。
已记录信息的组织形式如下:
- TOPIC{separator}SUBTOPIC{separator}CONTENT... // maybe truncated

### 输入对话
你将收到用户和助手之间的对话。对话格式为：
- [TIME] NAME: MESSAGE
其中NAME是ALIAS(ROLE)或仅ROLE，当ALIAS可用时，使用ALIAS来指代用户/助手。
MESSAGE是对话内容。
TIME是此消息发生的时间，因此你需要根据TIME转换消息中的日期信息（如有必要）。

## 输出格式
请使用Markdown无序列表格式输出你的记录结果。
例如：
```
- Jack画了一幅关于他孩子们的画。[提及于 2023/1/23] // event
- 用户的昵称是Jack，助手是Melinda。[提及于 2023/1/23] // info
- Jack提到他在Memobase工作，是一名软件工程师。[提及于 2023/1/23] // info
- Jack计划去健身房。[提及于 2023/1/23，计划定在 2023/1/24] // schedule
...
```
始终添加你记录的具体提及时间，如果可能的话也要添加事件发生时间。
记住，确保你的记录是纯正和简洁的，任何时间信息都应该移动到[TIME INFO]块中。

## 内容要求
- 你需要列出用户信息和日程安排
- {additional_requirements}

现在请执行你的任务。
"""
```

基于该提示词修改获得EVENT_RETRIEVAL_PROMPT(根据具体上下文删去冗余内容)。

#### search 改动(mem0\memory\main.py)

* 新增_search_event_vector_store函数，参考_search_vector_store, 把内部的向量数据库替换为存储事件记忆的event_vector_store。

* 最终的original_memories分别从存储Profile记忆的向量数据库和存储Event的向量数据库进行检索相关的记忆，并合并到一起。

#### update 改动

* 新增_create_event_memory，参考_create_memory，把内部的向量数据库替换为存储事件记忆的event_vector_store。

#### delete 改动

* 新增_delete_event_memory，参考_delete_memory，把内部的向量数据库替换为存储事件记忆的event_vector_store。
